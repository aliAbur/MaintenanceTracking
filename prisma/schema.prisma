generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

enum TicketStatus {
  Open
  Processing
  OnHold
  Closed
}

enum TicketPriority {
  Low
  Medium
  High
}

enum UserRole {
  Admin
  Employee
  Observer
}

model UserProfile {
  id         String     @id @default(uuid())
  email      String     @unique
  password   String?
  fullName   String?
  role       UserRole   @default(Employee)
  createdAt  DateTime   @default(now())
  
  tickets        Ticket[]   @relation("TicketAssignee")
  createdTickets Ticket[]   @relation("TicketCreator")
  auditLogs      AuditLog[] @relation("AuditLogModifier")
}

model Ticket {
  id            String         @id @default(uuid())
  customerName  String
  customerPhone String?
  panelType     String
  priority      TicketPriority @default(Low)
  specialNotes  String?
  assignedTo    String?
  createdBy     String?
  status        TicketStatus   @default(Open)
  images        String[]       @default([])
  createdAt     DateTime       @default(now())
  updatedAt     DateTime       @updatedAt

  assignee      UserProfile?   @relation("TicketAssignee", fields: [assignedTo], references: [id])
  creator       UserProfile?   @relation("TicketCreator", fields: [createdBy], references: [id])
  auditLogs     AuditLog[]
}

model AuditLog {
  id         String       @id @default(uuid())
  ticketId   String
  modifiedBy String
  action     String
  oldValue   String?
  newValue   String?
  createdAt  DateTime     @default(now())

  ticket     Ticket       @relation(fields: [ticketId], references: [id], onDelete: Cascade)
  modifier   UserProfile  @relation("AuditLogModifier", fields: [modifiedBy], references: [id])
}
